{
  "name": "relu",
  "version": "1.0.0",
  "description": "ReLU, the rectified linear unit",
  "main": "src/index.js",
  "scripts": {
    "test": "ava --verbose --timeout=10s",
    "test-no-cov": "ava --verbose --timeout=10s",
    "report-cov": "nyc report --reporter=text-lcov > coverage.lcov && codecov"
  },
  "files": [
    "lib/",
    "src/"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/kaelzhang/relu.git"
  },
  "keywords": [
    "relu"
  ],
  "engines": {
    "node": ">=4"
  },
  "author": "kaelzhang",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/kaelzhang/relu/issues"
  },
  "ava": {
    "files": [
      "test.js"
    ]
  },
  "devDependencies": {
    "ava": "^0.16.0",
    "nyc": "^11.1.0",
    "codecov": "^2.3.0"
  }
}
